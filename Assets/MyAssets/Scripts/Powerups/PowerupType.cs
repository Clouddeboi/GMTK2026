// Broad category used for UI grouping/filtering. The actual behaviour of a powerup lives in
// its PowerupEffect, not in this enum - this is just metadata for display/pool logic.
public enum PowerupType
{
    AbilityUnlock,
    StatUpgrade,
    Resource,
}
